<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:elevation="5dp"
>

    <TextView
            android:id="@+id/show_favorites_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:text="Показать избранные"
            android:clickable="true"
            android:focusable="true"
            android:textSize="16sp"
    />

    <TextView
            android:id="@+id/level_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/show_favorites_button"
            app:layout_constraintStart_toStartOf="parent"
            android:text="Уровень:"
            android:clickable="true"
            android:focusable="true"
            android:textSize="16sp"/>

    <TextView
            android:id="@+id/school_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/level_button"
            app:layout_constraintStart_toStartOf="parent"
            android:text="Школа:"
            android:clickable="true"
            android:focusable="true"
            android:textSize="16sp"/>


</androidx.constraintlayout.widget.ConstraintLayout>